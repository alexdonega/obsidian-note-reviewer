# =============================================================================
# Supabase Edge Functions - Environment Variables
# =============================================================================
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# For production (Supabase Dashboard):
#   Go to Project Settings > Edge Functions > Secrets
#   Add each variable as a secret
#
# =============================================================================

# -----------------------------------------------------------------------------
# Rate Limiting (Upstash Redis)
# -----------------------------------------------------------------------------
#
# Required for production rate limiting. When not configured, edge functions
# will use an in-memory sliding window fallback (suitable for development only).
#
# Get these from: https://console.upstash.com/
# 1. Create a Redis database (free tier available)
# 2. Copy the REST URL and Token from the "REST API" section
#
# Rate limits applied:
#   - batch-operations: 5 requests per minute (high-impact bulk operations)
#   - process-note: 20 requests per minute (single-note operations)
#
# IMPORTANT: In production, always configure these to enable distributed rate
# limiting across edge function instances.
#
UPSTASH_REDIS_URL=https://your-database.upstash.io
UPSTASH_REDIS_TOKEN=your-upstash-rest-token

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
#
# These are automatically set by Supabase when deploying edge functions.
# For local development with `supabase functions serve`, they are injected
# from your local Supabase configuration.
#
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
